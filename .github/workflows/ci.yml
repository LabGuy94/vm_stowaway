name: ci

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - run: make -j"$(sysctl -n hw.ncpu)"
      - run: make test
